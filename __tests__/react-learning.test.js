'use strict';

const reactLearning = require('..');
const assert = require('assert').strict;

assert.strictEqual(reactLearning(), 'Hello from reactLearning');
console.info("reactLearning tests passed");
